export const environment = {
  production: false,
  apiUrl: 'http://localhost:8081',
  /** Client ID PayPal public (sandbox) — identique aux variables d’environnement du backend. */
  paypalClientId: '' as string
};
